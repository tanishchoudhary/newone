name: called

on:
  push:
    branches: [ main ] 
jobs:
  bro:
  
    runs-on: ubuntu-latest
    steps:
      - name: 'checkout coode ${{ github.REF }}'
        id: calling
        run: |
          echo "done"
          exit 0
       
       
      - name: call
        #if: ${{contains(needs.steps.calling.result,'failure')}}
        run: echo "hello"
        
          
  
